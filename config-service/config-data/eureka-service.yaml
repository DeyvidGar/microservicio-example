server:
 port: 8761

eureka:
  instance:
    hostname: test
  client:
    fetchRegistry: false
    registerWithEureka: false
    service-url:
      default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/
